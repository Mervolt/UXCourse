import React from "react";
import Icon from "react-native-vector-icons/Entypo";

const MenuIcon = ({onPress}) => {
  return <Icon name="menu" size={23} color="#fff" onPress={onPress}/>;
};

export default MenuIcon;
